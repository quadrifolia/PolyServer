<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PolyServer - Server Ready</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #427f86 0%, #2a5a61 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            background: white;
            padding: 3rem;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            text-align: center;
            max-width: 600px;
            margin: 2rem;
        }
        h1 {
            color: #333;
            margin-bottom: 1rem;
            font-size: 2.5rem;
        }
        .status {
            color: #28a745;
            font-size: 1.2rem;
            margin-bottom: 2rem;
            font-weight: 600;
        }
        .info {
            color: #666;
            margin-bottom: 2rem;
        }
        .features {
            text-align: left;
            margin: 2rem 0;
        }
        .features h3 {
            color: #333;
            margin-bottom: 1rem;
        }
        .features ul {
            list-style: none;
            padding: 0;
        }
        .features li {
            padding: 0.5rem 0;
            color: #666;
        }
        .features li:before {
            content: "✓ ";
            color: #28a745;
            font-weight: bold;
        }
        .footer {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #eee;
            color: #999;
            font-size: 0.9rem;
        }
        .hostname {
            color: #007bff;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 PolyServer</h1>
        <div class="status">Server Successfully Configured</div>
        <div class="info">
            Your security-hardened Debian server foundation is ready for application deployment.
            <br><br>
            <strong>Server:</strong> <span class="hostname">{{BASE_DOMAIN}}</span>
        </div>
        
        <div class="features">
            <h3>Security Features Active:</h3>
            <ul>
                <li>UFW Firewall with fail2ban protection</li>
                <li>ModSecurity Web Application Firewall</li>
                <li>Suricata Network Intrusion Detection</li>
                <li>AppArmor mandatory access control</li>
                <li>Comprehensive audit framework</li>
                <li>Malware protection (ClamAV + maldet)</li>
                <li>File integrity monitoring (AIDE)</li>
                <li>Unbound DNS caching</li>
                <li>DSGVO/GDPR compliance toolkit</li>
            </ul>
        </div>

        <div class="info">
            <strong>Next Steps:</strong><br>
            Deploy your applications using the PolyServer foundation.<br>
            All security configurations are application-agnostic and ready to use.
        </div>
        
        <div class="footer">
            This default page will be replaced when you deploy your application.<br>
            <strong>Health Check:</strong> <a href="/health">/health</a>
        </div>
    </div>
</body>
</html>